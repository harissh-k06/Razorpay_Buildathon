# ==============================================================================
# PennyWise - Global Backend Python Requirements
# Install with: pip install -r requirements.txt
# ==============================================================================

# ── 1. Web Framework & API Server ──
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
python-multipart>=0.0.9
itsdangerous>=2.1.0
requests>=2.28.0
httpx>=0.27.0

# ── 2. Data Processing & Reconciliation Engine ──
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
python-dateutil>=2.8.0

# ── 3. AI, LLM & Model Context Protocol (FastMCP) ──
openai>=1.12.0
fastmcp>=0.1.0
mcp>=1.0.0

# ── 4. Environment & Data Validation ──
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# ── 5. Google OAuth 2.0 & Gmail Integration ──
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0
google-api-python-client>=2.0.0

# ── 6. Synthetic Data Generation & Utilities ──
razorpay>=2.0.0
faker>=20.0.0
tqdm>=4.65.0
